private void SerializeToXml(List<ButtonData> data, string filePath)
        {
            var serializer = new XmlSerializer(typeof(List<ButtonData>));
            using (var writer = new StreamWriter(filePath))
            {
                serializer.Serialize(writer, data);
            }
        }